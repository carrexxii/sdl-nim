import sdl/[init, events, mouse, rect, pixels, surface, video, renderer, opengl, properties, timer]
export      init, events, mouse, rect, pixels, surface, video, renderer, opengl, properties, timer

from sdl/common import get_error
export get_error
