import sdl/[init, events, mouse, rect, pixels, surface, video, opengl, vulkan, properties, timer, dialog]
export      init, events, mouse, rect, pixels, surface, video, opengl, vulkan, properties, timer, dialog

from sdl/common import get_error
export get_error
