import sdl/[init, iostream, events, mouse, surface, video, opengl, vulkan, properties, timer, dialog]
export      init, iostream, events, mouse, surface, video, opengl, vulkan, properties, timer, dialog

from sdl/common import get_error
export get_error
